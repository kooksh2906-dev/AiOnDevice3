# The common Base clock wizard and Basys3 board flow provide the 100 MHz
# sys_clock, reset, and USB UART constraints.  Comparison C has no additional
# external pins and deliberately adds no duplicate create_clock constraint.
